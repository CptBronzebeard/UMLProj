
<%categories.each do |cat|%>

  <%if cat.is_root?%>
  <ul>
    <%#byebug%>

    <%= render partial: "/products/cat", locals: {cat: cat,address:address,v_id:v_id,r:r} %>
    </ul>
  <% end %>
<% end %>
